[app]
title = Moonton Safe Checker
package.name = moonton_safe_checker
package.domain = org.example
source.dir = .
source.include_exts = py,png,jpg,kv,txt
version = 0.1
requirements = kivy
orientation = portrait
android.api = 33
android.minapi = 21
android.arch = armeabi-v7a
# (optional) set permissions to read external storage
android.permissions = READ_EXTERNAL_STORAGE,WRITE_EXTERNAL_STORAGE
# Include a simple icon if needed (not included)
# icon.filename = %(source.dir)s/icon.png